console.log('test');
alert('test');

var dataset = document.getElementById('rawdata');
dataset.split(',');
for (var datum in dataset)
{
    new Date(datum);
}
for (var temperatuur in dataset)
{
    //;
}
